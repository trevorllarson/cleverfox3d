{
    "name": "electricpulp",
    "version": "1.0.0",
    "main": "webpack.mix.js",
    "scripts": {
        "dev": "npm run dev --prefix assets/",
        "watch": "npm run watch --prefix assets/",
        "hot": "npm run host --prefix assets/",
        "production": "npm run production --prefix assets/"
    }
}
